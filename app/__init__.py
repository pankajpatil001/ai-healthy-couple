"""Healthy Couple — Foundation slice (Phase 1).

A modular monolith (FastAPI + PostgreSQL + Redis) providing the secure
application foundation: authentication & sessions, user accounts, couples &
invitations, and the server-side authorization / privacy-boundary layer.
"""

__version__ = "0.1.0"
